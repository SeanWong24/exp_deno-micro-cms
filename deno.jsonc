{
  "tasks": {
    "dev": "DEV=1 PASSCODE=test DB_PATH=./temp/db deno run -A --unstable --watch mod.ts",
    "dev:debugger": "DEV=1 PASSCODE=test DB_PATH=./temp/db deno run -A --unstable --watch --inspect-wait mod.ts",
    "start": "deno run --allow-net --allow-env --unstable --watch mod.ts",
    "docs:generate": "deno run --allow-env --allow-read --allow-write openapi.ts"
  },
  "exclude": ["/www", "/admin-ui"]
}
